# Local defaults
TIMETREE_EMAIL=you@example.com
TIMETREE_PASSWORD=app-password
TIMETREE_CALENDAR_CODE=abcdef123456

# Use local config file in project root (optional)
#TIMETREE_CONFIG=./config/config.yaml

# Token seed for deterministic filenames (optional)
TOKEN_SEED=local-seed

# Output paths
OUTPUT_BASE=./data
OUTPUT_PATH=./data/timetree.ics

# Scheduling
CRON_SCHEDULE=*/30 * * * *
STARTUP_DELAY=0s

# Server
PORT=8080

# Logging
LOG_LEVEL=debug
